applications:
  - name: calorie-counter
    memory: 256M
    buildpacks:
      - nodejs_buildpack
    command: node server/index.js
    env:
      NODE_ENV: production
    services:
      - calorie-counter-db
