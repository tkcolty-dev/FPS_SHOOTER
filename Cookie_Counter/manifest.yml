---
applications:
  - name: cookie-counter
    memory: 256M
    disk_quota: 512M
    buildpacks:
      - nodejs_buildpack
    command: node server/index.js
    services:
      - cookie-counter-db
    env:
      NODE_ENV: production
